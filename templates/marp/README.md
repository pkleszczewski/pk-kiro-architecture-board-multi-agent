# Corporate Templates

Drop your corporate Marp CSS themes here. They will be used by the documentalist agent when generating presentations.

## Usage

Place `.css` files in this directory. Reference them in presentation markdown:

```markdown
---
marp: true
theme: your-template-name
---
```

## Default

If no corporate template is provided, the `default.css` theme is used.
